# AI_VoiceControl_System

This project is a voice-controlled AI assistant built using Python that listens to user commands and performs tasks such as playing music, telling time/date, fetching information from Wikipedia, and retrieving stored contact or bank details.

It uses speech recognition for input and text-to-speech for output, enabling a hands-free interactive experience.
